version: '3.8'

services:
  app:
    build: .
    ports:
      - "5000:5000"
    environment:
      - DATABASE_URL=postgresql://smartur:F2YRLr2n4yFnaC4Y@db:5432/smartur
      - SESSION_SECRET=smartur-guvenli-oturum-2024
      - NODE_ENV=production
      - PORT=5000
      - SUPER_ADMIN_EMAIL=admin@example.com
      - SUPER_ADMIN_PASSWORD=admin123
    depends_on:
      - db
    restart: unless-stopped

  db:
    image: postgres:16-alpine
    environment:
      - POSTGRES_USER=smartur
      - POSTGRES_PASSWORD=F2YRLr2n4yFnaC4Y
      - POSTGRES_DB=smartur
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./smartur_backup.sql:/docker-entrypoint-initdb.d/init.sql
    restart: unless-stopped

volumes:
  postgres_data:
